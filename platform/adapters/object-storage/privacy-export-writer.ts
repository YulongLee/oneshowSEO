import{mkdir,rm,writeFile}from"node:fs/promises";import{dirname,join,resolve}from"node:path";import type{PrivacyArtifactWriter}from"../../modules/operations/privacy";
export class LocalPrivacyArtifactWriter implements PrivacyArtifactWriter{constructor(private readonly root:string){}private path(key:string){const root=resolve(this.root),path=resolve(join(root,key));if(!path.startsWith(`${root}/`))throw new Error("UNSAFE_OBJECT_KEY");return path;}async write(key:string,body:Uint8Array){const path=this.path(key);await mkdir(dirname(path),{recursive:true,mode:0o700});await writeFile(path,body,{mode:0o600});}async remove(key:string){await rm(this.path(key),{force:true});}}
