import { getDatabase } from "./auth";import{ensureProductSchema}from"./product";import{SqliteIntegrationConnectionRepository}from"../platform/adapters/sqlite/integration-connection-repository";import{IntegrationConnectionService}from"../platform/modules/integrations/connections";import{wordpressAdapter,type ProviderCredentialResolver,type ProviderHttpClient}from"../platform/modules/integrations/launch-adapters";import{SafeProviderHttpClient,type SafeHttpRequest}from"../platform/modules/integrations/safe-http";import{integrationSecretVault}from"../platform/modules/integrations/vault";
export async function integrationRepository(){await ensureProductSchema();const repository=new SqliteIntegrationConnectionRepository(getDatabase());repository.ensureSchema();return repository;}
export async function integrationRuntime(){const repository=await integrationRepository(),vault=integrationSecretVault();const credentials:ProviderCredentialResolver={async withAuthorization<T>(handle:string,operation:(authorization:string)=>Promise<T>){const record=repository.credentialContext(handle);if(!record)throw new Error("INTEGRATION_CREDENTIAL_UNAVAILABLE");return vault.withDecrypted(record.encryptedEnvelope,{organizationId:record.organizationId,projectId:record.projectId,connectionId:record.connectionId,recordId:record.id,recordVersion:record.recordVersion,purpose:"provider_credential"},secret=>operation(new TextDecoder().decode(secret)));}};const http:ProviderHttpClient={async request(input:SafeHttpRequest){const url=new URL(input.url);const client=new SafeProviderHttpClient({allowedHosts:[url.hostname],credentialHosts:[url.hostname],timeoutMs:10_000,maxRedirects:2,maxRetries:2,maxResponseBytes:2_000_000,circuitFailureThreshold:3,circuitResetSeconds:60});return client.request(input);}};const adapters=new Map([["wordpress",wordpressAdapter(http,credentials)]]);return{repository,service:new IntegrationConnectionService(repository,vault,adapters)};}
