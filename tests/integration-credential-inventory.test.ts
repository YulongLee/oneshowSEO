import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("credential inventory records the no-credential production result and future least-privilege gate",async()=>{const evidence=await readFile(new URL("../docs/releases/2026-08-06-integration-credential-inventory.md",import.meta.url),"utf8");for(const expected of[/no Aliyun or OSS access-key variables/,/contain no encrypted configuration/,/dedicated least-privilege account/,/old-key request is denied/,/No secret values/])assert.match(evidence,expected);assert.doesNotMatch(evidence,/AKID[A-Za-z0-9]{8,}|LTAI[A-Za-z0-9]{8,}/);});
test("artifact storage enforces the dedicated tenant/project oneshowseo prefix",async()=>{const source=await readFile(new URL("../platform/modules/execution/object-storage.ts",import.meta.url),"utf8");assert.match(source,/`oneshowseo\/\$\{input\.organizationId\}\/\$\{input\.projectId\}\/\$\{input\.taskId\}\/\$\{input\.artifactId\}\//);assert.match(source,/startsWith\(prefix\)/);});
