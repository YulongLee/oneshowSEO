"use client";import{useEffect,useState}from"react";type Sli={key:string;state:"healthy"|"breached"|"insufficient_data";targetPercent:number;actualPercent:number|null;samples:number;failures:number};export function ObservabilityConsole(){const[slis,setSlis]=useState<Sli[]|null>(null),[denied,setDenied]=useState(false);useEffect(()=>{const timer=setTimeout(async()=>{const response=await fetch("/api/admin/operations",{cache:"no-store"}),data=await response.json();if(!response.ok||!data.observability)return setDenied(true);setSlis(data.observability.slis)},0);return()=>clearTimeout(timer)},[]);return <section className="admin-panel"><div className="admin-panel-title"><div><h2>SLI / SLO 与告警</h2><p>基于真实结构化事件计算；样本不足时不推算可用率。</p></div></div>{denied?<p>当前角色无权查看。</p>:!slis?<p>正在读取可观测性数据…</p>:slis.map(item=><p key={item.key}><b>{item.key}</b> · {item.state==="insufficient_data"?"数据不足":item.state==="healthy"?"健康":"违反目标"} · {item.actualPercent===null?"—":`${item.actualPercent}%`} / 目标 {item.targetPercent}% · 样本 {item.samples}</p>)}</section>}
