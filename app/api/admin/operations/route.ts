import{NextResponse}from"next/server";import{getCurrentUser}from"../../../../lib/auth";import{authorizeOperator,operatorDashboardSnapshot}from"../../../../lib/operator-administration";import{OperatorAuthorizationError,operatorPermissions}from"../../../../platform/modules/operations/administration";
export async function GET(request:Request){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"请先登录"},{status:401});try{const authorization=await authorizeOperator(user,"tenants.read"),query=new URL(request.url).searchParams.get("q")??"";return NextResponse.json(await operatorDashboardSnapshot(query,operatorPermissions(authorization.role)),{headers:{"cache-control":"private, no-store"}});}catch(error){if(error instanceof OperatorAuthorizationError)return NextResponse.json({error:"无权访问后台运营数据"},{status:403});throw error;}}
