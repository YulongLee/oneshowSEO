import{NextResponse}from"next/server";import{agentStatus}from"../../../../lib/agent-status";import{getCurrentUser}from"../../../../lib/auth";import{ownedProject}from"../../../../lib/product";import{can,permissions,type OrganizationRoleKey}from"../../../../platform/modules/identity/authorization";export async function GET(request:Request){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"请先登录"},{status:401});if(!can(user.organization.roleKey as OrganizationRoleKey,permissions.projectsRead))return NextResponse.json({error:"没有查看 Agent 状态的权限"},{status:403});const projectId=new URL(request.url).searchParams.get("projectId")||"";if(!await ownedProject(user.organization.organizationId,projectId))return NextResponse.json({error:"项目不存在"},{status:404});return NextResponse.json({agents:(await agentStatus()).list(user.organization.organizationId,projectId)},{headers:{"cache-control":"private, no-store"}});}
